// Syncthing ignore patterns for the EXTRA_SYNC_DIR (typically a code/projects dir)
// Deployed by the bootstrap scripts as <EXTRA_SYNC_DIR>/.stignore

// Node / JavaScript
node_modules
.next
.nuxt
.turbo
dist
build
out
.cache
.parcel-cache
.svelte-kit
*.tsbuildinfo

// Python
__pycache__
*.pyc
*.pyo
.venv
venv
env
.pytest_cache
.mypy_cache
.ruff_cache

// Rust / Go
target
vendor

// Java / Gradle
.gradle
*.class
*.jar

// Flutter / Dart
.dart_tool
.flutter-plugins
.flutter-plugins-dependencies

// Logs & dbs
*.log
*.sqlite-journal

// Cryptographic keys / certs (never sync)
*.pem
*.key

// .env files: NOT excluded by default. They're per-project config that's
// useful to have on every device you work from. Add `.env` to this file
// if you store production secrets in there and don't want them on every
// machine. (Lockfiles / source code are synced regardless.)

// IDE
.idea
.vscode
*.swp

// OS
.DS_Store
Thumbs.db
desktop.ini

// Syncthing
.stversions
.stfolder
